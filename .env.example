# Copiá a ".env.local" (Next.js lo lee solo). Todo opcional.

# Secreto compartido con el bot de WhatsApp (carpeta bot-whatsapp).
# Si lo definís, el bot DEBE mandar el mismo valor en el header
# "x-bot-token" para poder usar /api/bot-whatsapp/*. Vacío / sin definir =
# las rutas del bot quedan abiertas (sólo para desarrollo en la misma PC).
BOT_TOKEN=
